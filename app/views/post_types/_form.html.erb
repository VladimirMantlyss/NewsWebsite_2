<%= form_with(model: post_type) do |form| %>
  <% if post_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post_type.errors.count, "error") %> prohibited this post_type from being saved:</h2>

      <ul>
        <% post_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :Тип_поста, class: "form-label" %>
    <%= form.text_field :type, class: "form-control col-md-3" %>
  </div>

  <div>
    <%= form.submit "Виконати", class: "btn btn-primary" %>
  </div>
<% end %>
