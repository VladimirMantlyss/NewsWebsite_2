<p style="color: green"><%= notice %></p>

<h1>Типи постів</h1>

<div id="News">
  <table class="table table table-striped table table-bordered table table-hover">
    <thead class="thead-dark">
    <tr>
      <th>Тип</th>
      <th colspan="3"></th>
    </tr>
    </thead>
    <tbody>
    <% @post_types.each do |postType| %>
      <tr>
        <td><%= postType.type %></td>
        <td>
          <%#= link_to "Редагувати", edit_post_path(post), class:"btn btn-secondary" %>
          <%= link_to "Редагувати", post_type_path(postType), class: "btn btn-danger", method: :delete, data: { confirm: "Вы уверены?" } %>
          <%#= link_to "Show", patient %>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>

<!--<div id="posts">-->
<%# @posts.each do |post| %>
<%#= render post %>
<!--    <p>-->
<%#= link_to "Детальніше", post %>
<!--    </p>-->
<%# end %>
<!--</div>-->

<%= link_to "Створити новий пост", new_post_type_path %>
