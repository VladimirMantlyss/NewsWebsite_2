<%= form_with(model: keyword) do |form| %>
  <% if keyword.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(keyword.errors.count, "error") %> prohibited this keyword from being saved:</h2>

      <ul>
        <% keyword.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :Ключове_слово, class: "form-label" %>
    <%= form.text_field :keyword, class: "form-control col-md-3" %>
  </div>
  <br>
  <div>
    <%= form.submit "Виконати", class: "btn btn-primary" %>
  </div>
<% end %>
