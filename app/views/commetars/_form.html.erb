<%= form_with(model: commetar) do |form| %>
  <% if commetar.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(commetar.errors.count, "error") %> prohibited this commetar from being saved:</h2>

      <ul>
        <% commetar.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :comentar_text, style: "display: block" %>
    <%= form.text_field :comentar_text %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
